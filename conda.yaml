name: churn_prediction_env
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.9
  - pip
  - pip:
      - mlflow
      - catboost
      - pandas
      - numpy
      - scikit-learn
      - matplotlib
      - seaborn
      - ipywidgets
